<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="common_line_horizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.1dp</item>
        <item name="android:background">@color/line_bg_cccccc</item>
    </style>

    <style name="common_edittext_textcolor">
        <item name="android:textColor">@color/black_normal_text</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColorHint">@color/black_small_text</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="common_button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">42dp</item>
        <item name="android:background">@drawable/common_btn_shape_red</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="common_button_small">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:background">@drawable/common_btn_shape_small_red</item>
        <item name="android:textColor">@color/theme_red</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- 登录注册忘记密码 -->
    <style name="style_login_edittext" parent="@style/common_edittext_textcolor">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@color/white</item>
        <item name="android:textColor">#666666</item>
        <item name="android:textColorHint">#cccccc</item>
        <item name="android:drawablePadding">13dp</item>
        <item name="android:paddingLeft">13dp</item>
    </style>

</resources>